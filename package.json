{
  "name": "hyperliquid-noti",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Scheduled Hyperliquid price alerts via Cloudflare Workers, Supabase, and Poke.",
  "scripts": {
    "dev": "npx wrangler dev --test-scheduled",
    "start": "npx wrangler dev --test-scheduled",
    "deploy": "npx wrangler deploy",
    "test": "node --test",
    "cf:typegen": "npx wrangler types"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "wrangler": "^4.37.1"
  }
}
